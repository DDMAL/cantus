apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: cantus-ingress
  namespace: cantus-ultimus-staging
spec:
  rules:
  - host: cantus.staging.simssa.ca
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: nginx
            port:
              number: 8000