apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mei-files-staging-pvc
  namespace: cantus-ultimus
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: ""
  volumeName: mei-files-staging-pv
  resources:
    requests:
      storage: 20Gi
