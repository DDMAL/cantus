apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cantaloupe-manifests-pvc
  namespace: cantus-ultimus
spec:
  accessModes: [ReadWriteOnce]
  resources:
    requests:
      storage: 5Gi
  storageClassName: standard